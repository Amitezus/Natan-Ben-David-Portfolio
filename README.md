# Natan Ben-David — Portfolio

Live at **natanuxi.com** — served by GitHub Pages from this repo's root
(`index.html` + `assets/` + `images/`).

See [`DEVELOPMENT.md`](DEVELOPMENT.md) for how the page is built: the content
model, the streaming-text engine, the 4px design scale, and the bilingual
language handling.

This repo holds only the images the site actually uses — a pruned copy of a
much larger Webflow media export.
